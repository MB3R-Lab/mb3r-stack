# Jenkins Adapter

`ci/jenkins/` is the Jenkins Shared Library base path for this repository.

Configure Jenkins to load the library from SCM and set the library path to `ci/jenkins`.

See `docs/install-jenkins.md` and `examples/jenkins/Jenkinsfile`.
