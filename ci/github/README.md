# GitHub Adapter

GitHub reusable workflows live in `.github/workflows/`.

This directory is reserved for GitHub-specific docs and notes so the repository keeps a clean separation between:

- platform-neutral release metadata and scripts
- GitHub-specific automation wrappers

See `docs/install-github.md` and `examples/github/example-caller.yml`.
