# GitLab Adapter

GitLab CI/CD Catalog-compatible components are stored in the repository root `templates/` directory.

That layout is required by GitLab component projects. This directory exists for GitLab-focused docs, examples, and repository-local context.

See `docs/install-gitlab.md` and `examples/gitlab/.gitlab-ci.yml`.
